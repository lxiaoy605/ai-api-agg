apiVersion: 1

providers:
  - name: "API Hub Dashboards"
    orgId: 1
    folder: "API Hub"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
