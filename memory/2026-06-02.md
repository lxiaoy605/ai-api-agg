# 2026-06-02

## 邮件系统重构 ✅

- Worker 极简化：从 240 行减到 35 行，只转发原始 EML 到后端 + Gmail forward
- 后端接管 MIME 解析：Go `net/mail` 标准库，`parser.go` 新建
- 新增 `POST /api/email/raw` 端点（接受原始 EML text/plain）
- Telegram 通知从 Worker 移到后端（`notifyNewEmail`）
- 修复 DKIM 头误匹配 bug：正则加 `^` 行首锚点
- 修复非 multipart base64 不解码 bug
- 修复 `/emails_list` 路由到 help 的 bug → 路由到 cmdList
- LIMIT 从 10 改为 50
- 修复 Telegram HTML 模式下邮件内容未转义导致发送失败

## Gmail 配置

- 配好 support@aiflowhub.ai 和 user@aiflowhub.ai 的 Gmail "Send as"
- `message.forward()` → lxiao605@gmail.com
- 修复 Gmail 确认邮件 Base64 无法解码问题（从 DB 提取完整 base64 解码）

## 问题 3

- 邮件触发通知 → 发送给用户（注册确认、余额预警、新模型公告）→ 待新会话实现

## 运维文档更新

- 运维手册新增：邮件系统架构、Cloudflare Worker 配置、Mailgun 配置、Gmail Send-as 步骤、数据持久化说明、当前工作摘要

## 环境变量

Cloudflare Worker:
- BACKEND_URL = https://aiflowhub.ai/api/email/raw（已改为 /raw）
- INBOUND_SECRET（与后端一致）
- FORWARD_EMAIL = lxiao605@gmail.com
